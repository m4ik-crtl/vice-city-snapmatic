# Optional — only needed to unlock the in-editor AI Assistant.
# Create a free project at https://unlayer.com and paste its numeric Project ID.
# The core editor (crop, resize, filters, draw, text, shapes, stickers, frames)
# works WITHOUT this. Copy this file to ".env" and fill it in to enable AI.
VITE_UNLAYER_PROJECT_ID=
